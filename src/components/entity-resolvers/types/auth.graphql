type JwtResponse {
    accessToken: String!
    refreshToken: String!
    expiresIn: Int!
}

type Mutation {
    login(input: LoginInput!): JwtResponse!
    exchangeToken(refreshToken: String!): JwtResponse!
}

input LoginInput {
    email: String!
    password: String!
}