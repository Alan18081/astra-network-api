type JwtResponse {
    accessToken: String
    refreshToken: String
    expiresIn: Long
}

type Mutation {
    login(input: LoginInput!): JwtResponse!
    changePassword(input: ChangePasswordInput!): User!

}

input LoginInput {
    email: String!
    password: String!
}